// George's asset switch. Empty = the procedural placeholder cat (GeorgeView draws it); set to the
// processed GLB path (written by `npm run assets -- --george <dir>`) to load the real George.
export const GEORGE_GLB: string = "";
